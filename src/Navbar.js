import React, { Component } from "react";
import "index.scss";

class Navbar extends Component {
  render() {
    return (
        <button>
          Home
        </button>
    );
  }
}

export default Navbar;
